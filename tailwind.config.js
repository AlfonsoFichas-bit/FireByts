/** @type {import('tailwindcss').Config} */
// Tailwind v4: theme tokens are defined in CSS via @theme. Keep config minimal.
export default {
  theme: {
    extend: {
      // Add any non-color extensions here if needed
    },
  },
  plugins: [],
};